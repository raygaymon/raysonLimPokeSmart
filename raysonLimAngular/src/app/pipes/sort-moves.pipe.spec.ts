import { SortMovesPipe } from './sort-moves.pipe';

describe('SortMovesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortMovesPipe();
    expect(pipe).toBeTruthy();
  });
});
