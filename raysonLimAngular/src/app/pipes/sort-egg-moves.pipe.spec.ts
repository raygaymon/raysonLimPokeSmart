import { SortEggMovesPipe } from './sort-egg-moves.pipe';

describe('SortEggMovesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortEggMovesPipe();
    expect(pipe).toBeTruthy();
  });
});
