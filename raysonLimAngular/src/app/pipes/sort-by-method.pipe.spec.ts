import { SortByMethodPipe } from './sort-by-method.pipe';

describe('SortByMethodPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByMethodPipe();
    expect(pipe).toBeTruthy();
  });
});
