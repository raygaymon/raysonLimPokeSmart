import { SortTutorPipe } from './sort-tutor.pipe';

describe('SortTutorPipe', () => {
  it('create an instance', () => {
    const pipe = new SortTutorPipe();
    expect(pipe).toBeTruthy();
  });
});
